## Project Layout
- Root: `compose.yaml`, `AGENT.md`
- Web Service: `./web/` (contains Flask app and uploads)
- Worker Service: `./worker/` (contains conversion scripts and output)
- **Command Explanation:** Before suggesting any terminal command, briefly explain what each flag (e.g., -v, -u, -p) does in the context of our Debian/Docker environment.
